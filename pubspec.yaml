name: even_realities_g1
description: A Dart library for controlling Even Realities G1 smart glasses via Bluetooth LE. Supports display, notifications, navigation, voice control, and more.
version: 0.1.0
homepage: https://github.com/emingenc/even_realities_g1
repository: https://github.com/emingenc/even_realities_g1
issue_tracker: https://github.com/emingenc/even_realities_g1/issues

environment:
  sdk: ^3.0.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.34.5
  crc: ^0.0.2
  collection: ^1.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  # This package does not include any assets
