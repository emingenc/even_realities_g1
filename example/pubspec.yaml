name: even_realities_g1_example
description: Example app demonstrating the Even Realities G1 library.
publish_to: 'none'

version: 1.0.0

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  even_realities_g1:
    path: ../
  http: ^1.2.0
  path_provider: ^2.1.5
  shared_preferences: ^2.3.4
  # Local whisper using whisper.cpp (no ffmpeg dependency!)
  whisper_flutter_new: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
